name: skill_slash_commands
title: Skill Slash Commands
description: Exposes selected current-context Agent Zero skills as direct slash commands, using Commands when available and a lightweight fallback otherwise.
version: 1.3.0
settings_sections:
  - agent
per_project_config: true
per_agent_config: true
